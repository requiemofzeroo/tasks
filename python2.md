# **Домашнее задание**
## **Курс: Переменные, Операторы и Логика для QA**
### **Выполнил студент группы QA511: Курбатов Максим Вадимович**

## Первое задание:

**Посмотрите на этот код. Не запуская его на компьютере, определите, какое значение (Истина или Ложь) будет лежать в переменной is_test_passed в самом конце? Запишите свой ответ и ход рассуждений на листочке, а затем проверьте себя, запустив код в IDE.**

bugs_found = 3

critical_bugs = 1

execution_time = 45 # секунды

is_test_passed = (bugs_found < 5 and not critical_bugs == 1) or execution_time <= 60

critical_bugs == 1 - True

bugs_found < 5 - True

(bugs_found < 5 and not critical_bugs == 1) - False

execution_time <= 60 - True

is_test_passed - True


## Второе задание:

**Младший тестировщик написал скрипт, который должен рассчитывать общую стоимость билетов на концерт для группы тестировщиков с учетом скидки по промокоду. Но код не работает: он либо падает с ошибками, либо считает неправильно.
Скопируйте код к себе, найдите 3 ошибки (синтаксическую, ошибку выполнения и логическую), исправьте их и сделайте код рабочим.**
 
# Исходные данные (менять нельзя)

ticket_price = "1500"  

quantity = 4           

discount = 200         

total_sum = int(ticket_price) * quantity - discount

print(f"Вы покупаете билеты в количестве: {quantity} шт.")

print(f"Итоговая сумма к оплате с учетом скидки: {total_sum} руб.")

## Третье задание:

**Написание скрипта «Калькулятор покрытия тестами»**
**Напишите программу для тест-менеджера с нуля по следующим требованиям:**
 
   1. Программа должна запросить у пользователя (через консоль) общее количество требований в проекте (используйте input()).
   2. Затем программа должна запросить количество требований, которые уже покрыты автотестами.
   3. Программа должна рассчитать процент покрытия (формула: покрытые требования / всего требований * 100).
   4. Выведите в консоль красивый лог с помощью f-строки ровно в таком формате (вместо чисел должны подставляться ваши расчеты):
   [INFO] Всего требований: 80. Покрыто тестами: 60. Процент покрытия: 75.0%
   5. Дополнительный вопрос на засыпку: Что произойдет с вашей программой, если запустить её и ввести в первом поле 0? Какую ошибку вы увидите?

**Код скрипта:**

project_requriments = int(input("Введите общее кол-во требований в проекте: "))

covered_requriments = int(input("Введите кол-во требований, которые покрыты автотестами: "))

cover_percent = (covered_requriments / project_requriments) * 100

print(f"[INFO] Всего требований: {project_requriments}. Покрыто тестами: {covered_requriments}. Проецент покрытия: {cover_percent}%")

Ответ на доп. вопрос:
Программа попытается поделить на 0, в следствии чего и выдаст соответствующую ошибку.

## Четвертое задание:

**Представьте, что вы тестируете интернет-магазин. На одной странице помещается ровно 10 товаров.**
**Напишите программу, которая:**
 
   1. Запрашивает у пользователя общее количество товаров на складе (например, студент вводит 43).
   2. Рассчитывает и выводит на э	кран, сколько товаров останется на последней (неполной) странице (для числа 43 ответ должен быть 3). Подсказка: используйте правильный математический оператор из урока

total_products = int(input("Введите общее кол-во товаров на складе: "))

products_on_page = 10

stock_balance = total_products % products_on_page

print(f"На последней (неполной) странице останется товаров: {stock_balance} шт.")
 






